// Package utils provides shared constants and utilities
package utils

// MaxCommandLength defines the maximum command length that can be
// executed directly without using script files.
// Commands longer than this require an explicit shell definition.
const MaxCommandLength = 1000
